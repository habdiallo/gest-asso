// eslint-disable-next-line @typescript-eslint/consistent-type-imports -- `typeof` needs a value import, not a type-only one.
import fr from '../../../assets/i18n/fr.json';

export type TranslationKey = keyof typeof fr;
